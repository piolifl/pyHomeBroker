Pasos:
pip install virtualenv
virtualenv env
    Creamos: .gitignore // https://github.com/github/gitignore/blob/main/Python.gitignore
    Creamos: requirements.txt // django==3.2.6
Con el env activo ejecutar: pip install -r requirements.txt

Seguirdad y proteccion todas las password:

Instalar con requirements.txt django-environ==0.4.5

core 
    Creamos: .env
    .env
        SECRET_KEY=djct3%nrbuk6w8mwr(ak0-&3sd1omy3u+aikake_l)qf80rvan
        #Clave sin espacios sin comillas
        DEBUG=True

    settings.py #Agregamos
        import environ
        env = environ.Env()
        environ.Env.read_env()

        SECRET_KEY = os.environ.get('SECRET_KEY')
        #Asi se guarda la clave y se evita perderla



